# Java Stream API 
Stream is an object that references a sequence of other objects which are all of the same type.
Is similar to a classical collection like a list or a set, but are some important differences which will be on the code of this repo.  
